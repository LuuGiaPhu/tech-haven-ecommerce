<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Quản Lý Đơn Hàng - ADMIN - TECH HAVEN</title>
    
    <!-- Google Fonts -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800&family=Roboto:wght@300;400;500;700;900&display=swap" rel="stylesheet">
    
    <!-- Font Awesome -->
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    
    <!-- Lottie Animation Library -->
    <script src="https://cdnjs.cloudflare.com/ajax/libs/lottie-web/5.12.2/lottie.min.js"></script>
    
    <!-- Firebase SDK -->
    <script src="https://www.gstatic.com/firebasejs/9.23.0/firebase-app-compat.js"></script>
    <script src="https://www.gstatic.com/firebasejs/9.23.0/firebase-auth-compat.js"></script>
    <script src="https://www.gstatic.com/firebasejs/9.23.0/firebase-firestore-compat.js"></script>
    
    <!-- Styles -->
    <link rel="stylesheet" href="/css/style.css">
    <link rel="stylesheet" href="/css/bill_detail_admin.css">
    
    <!-- Firebase Configuration for Admin Bill Management -->
    <script>
        // Initialize Firebase for admin bill management page
        if (!firebase.apps || firebase.apps.length === 0) {
            const firebaseConfig = {
                apiKey: "AIzaSyD_QGS7c0FYmNiMYxEHbtWuM1c5r5gUxsg",
                authDomain: "tech-haven-5368b.firebaseapp.com",
                projectId: "tech-haven-5368b",
                storageBucket: "tech-haven-5368b.firebasestorage.app",
                messagingSenderId: "442337591630",
                appId: "1:442337591630:web:6d31a1fa9cd20e7d0fdb7f",
                measurementId: "G-2CV1C2KHXZ"
            };
            firebase.initializeApp(firebaseConfig);
            console.log('✅ Firebase initialized for admin bill management page');
        }
        
        // Check admin status on page load (similar to revenue.ejs)
        document.addEventListener('DOMContentLoaded', async function() {
            console.log('📋 Admin Bill Management page loaded, checking admin status...');
            
            // Check if user has admin token from URL
            const urlParams = new URLSearchParams(window.location.search);
            const tokenFromUrl = urlParams.get('token');
            
            // Store token if provided
            if (tokenFromUrl) {
                localStorage.setItem('idToken', tokenFromUrl);
                console.log('🔑 Token saved from URL');
            }
            
            // Get stored token
            const storedToken = localStorage.getItem('idToken');
            
            if (!storedToken) {
                console.log('⚠️ No token found, redirecting to home...');
                alert('⚠️ Vui lòng đăng nhập để truy cập trang này');
                window.location.href = '/';
                return;
            }
            
            // Check admin status from API (fresh data)
            try {
                console.log('🔍 Verifying admin status from server...');
                const response = await fetch('/api/user', {
                    headers: {
                        'Authorization': `Bearer ${storedToken}`,
                        'Accept': 'application/json'
                    },
                    credentials: 'include'
                });
                
                if (!response.ok) {
                    console.error('❌ API verification failed:', response.status);
                    alert('⚠️ Phiên đăng nhập đã hết hạn. Vui lòng đăng nhập lại.');
                    window.location.href = '/';
                    return;
                }
                
                const data = await response.json();
                console.log('👤 User from API:', data.user?.name, '- Admin:', data.user?.is_admin);
                
                const isAdmin = data.user && (data.user.is_admin === true || data.user.isAdmin === true);
                
                if (!isAdmin) {
                    console.log('⚠️ User not admin, redirecting to home...');
                    alert('⚠️ Bạn không có quyền admin để truy cập trang này');
                    window.location.href = '/';
                    return;
                }
                
                // Store user data with admin status for future use
                if (data.user) {
                    localStorage.setItem('userData', JSON.stringify(data.user));
                    window.currentUser = data.user;
                    window.currentUser.isAdmin = true; // Ensure isAdmin flag is set
                    console.log('💾 Admin user data saved to localStorage');
                }
                
                console.log('✅ Admin access confirmed');
                
            } catch (error) {
                console.error('❌ Error verifying admin:', error);
                alert('⚠️ Không thể xác thực quyền truy cập. Vui lòng thử lại.');
                window.location.href = '/';
            }
        });
    </script>
</head>
<body class="bill-detail-page admin-page">
    <!-- Header -->
    <header class="header">
        <nav class="navbar">
            <div class="nav-brand">
                <h1><a href="/" style="text-decoration: none; color: inherit;">TECH HAVEN - ADMIN</a></h1>
            </div>
            
            <!-- Mobile Menu Toggle -->
            <div class="mobile-menu-toggle">
                <i class="fas fa-bars"></i>
            </div>
            
            <div class="nav-menu">
                <a href="/">TRANG CHỦ</a>
                <a href="#" onclick="navigateWithToken('/edit'); return false;">QUẢN LÝ SẢN PHẨM</a>
                <a href="#" onclick="navigateWithToken('/admin/bill'); return false;">QUẢN LÝ ĐỚN HÀNG</a>
                <a href="#" onclick="navigateWithToken('/revenue'); return false;">DOANH THU</a>
            </div>
        </nav>
    </header>
    
    <script>
        // Function to navigate with token
        function navigateWithToken(path) {
            const token = localStorage.getItem('idToken');
            if (token) {
                window.location.href = `${path}?token=${token}`;
            } else {
                alert('⚠️ Phiên đăng nhập đã hết hạn. Vui lòng đăng nhập lại.');
                window.location.href = '/';
            }
        }
    </script>

    <!-- Main Content -->
    <div class="bill-container">
        <!-- Page Header -->
        <div class="page-header">
            <div class="header-content">
                <button class="back-btn" onclick="window.history.back()">
                    <i class="fas fa-arrow-left"></i>
                </button>
                <div class="header-text">
                    <h1 class="page-title">
                        <i class="fas fa-tasks"></i>
                        Quản Lý Đơn Hàng
                    </h1>
                    <p class="page-subtitle">Xem và quản lý tất cả đơn hàng của khách hàng</p>
                </div>
            </div>
        </div>

        <!-- Filter & Stats -->
        <div class="bill-stats">
            <div class="stat-card stat-all">
                <div class="stat-icon">
                    <i class="fas fa-shopping-bag"></i>
                </div>
                <div class="stat-info">
                    <div class="stat-number" id="totalOrders">0</div>
                    <div class="stat-label">Tổng Đơn</div>
                </div>
            </div>
            
            <div class="stat-card stat-pending">
                <div class="stat-icon">
                    <i class="fas fa-clock"></i>
                </div>
                <div class="stat-info">
                    <div class="stat-number" id="pendingOrders">0</div>
                    <div class="stat-label">Chờ Xử Lý</div>
                </div>
            </div>
            
            <div class="stat-card stat-processing">
                <div class="stat-icon">
                    <i class="fas fa-spinner"></i>
                </div>
                <div class="stat-info">
                    <div class="stat-number" id="processingOrders">0</div>
                    <div class="stat-label">Đang Xử Lý</div>
                </div>
            </div>
            
            <div class="stat-card stat-shipping">
                <div class="stat-icon">
                    <i class="fas fa-truck"></i>
                </div>
                <div class="stat-info">
                    <div class="stat-number" id="shippingOrders">0</div>
                    <div class="stat-label">Đang Vận Chuyển</div>
                </div>
            </div>
            
            <div class="stat-card stat-delivered">
                <div class="stat-icon">
                    <i class="fas fa-box-open"></i>
                </div>
                <div class="stat-info">
                    <div class="stat-number" id="deliveredOrders">0</div>
                    <div class="stat-label">Đã Giao</div>
                </div>
            </div>
            
            <div class="stat-card stat-completed">
                <div class="stat-icon">
                    <i class="fas fa-check-circle"></i>
                </div>
                <div class="stat-info">
                    <div class="stat-number" id="completedOrders">0</div>
                    <div class="stat-label">Hoàn Thành</div>
                </div>
            </div>
            
            <div class="stat-card stat-cancelled">
                <div class="stat-icon">
                    <i class="fas fa-times-circle"></i>
                </div>
                <div class="stat-info">
                    <div class="stat-number" id="cancelledOrders">0</div>
                    <div class="stat-label">Đã Hủy</div>
                </div>
            </div>
            
            <div class="stat-card stat-returned">
                <div class="stat-icon">
                    <i class="fas fa-undo"></i>
                </div>
                <div class="stat-info">
                    <div class="stat-number" id="returnedOrders">0</div>
                    <div class="stat-label">Đã Trả Hàng</div>
                </div>
            </div>
        </div>

        <!-- Filter Tabs -->
        <div class="filter-tabs">
            <button class="filter-tab active" data-filter="all">
                <i class="fas fa-list"></i>
                Tất Cả
            </button>
            <button class="filter-tab" data-filter="pending">
                <i class="fas fa-clock"></i>
                Chờ Xử Lý
            </button>
            <button class="filter-tab" data-filter="processing">
                <i class="fas fa-spinner"></i>
                Đang Xử Lý
            </button>
            <button class="filter-tab" data-filter="shipping">
                <i class="fas fa-truck"></i>
                Đang Vận Chuyển
            </button>
            <button class="filter-tab" data-filter="delivered">
                <i class="fas fa-box-open"></i>
                Đã Giao
            </button>
            <button class="filter-tab" data-filter="completed">
                <i class="fas fa-check-circle"></i>
                Hoàn Thành
            </button>
            <button class="filter-tab" data-filter="cancelled">
                <i class="fas fa-times-circle"></i>
                Đã Hủy
            </button>
            <button class="filter-tab" data-filter="returned">
                <i class="fas fa-undo"></i>
                Đã Trả Hàng
            </button>
        </div>

        <!-- Bills List -->
        <div class="bills-list" id="billsList">
            <!-- Loading State -->
            <div class="loading-state" id="loadingState">
                <div class="loading-spinner"></div>
                <p>Đang tải đơn hàng...</p>
            </div>

            <!-- Empty State -->
            <div class="empty-state" id="emptyState" style="display: none;">
                <div class="empty-icon">
                    <i class="fas fa-shopping-bag"></i>
                </div>
                <h3>Chưa Có Đơn Hàng</h3>
                <p>Hiện tại chưa có đơn hàng nào trong hệ thống.</p>
            </div>

            <!-- Bills will be rendered here by JavaScript -->
        </div>
    </div>

    <!-- Bill Detail Modal -->
    <div class="modal-overlay" id="billDetailModal">
        <div class="modal-content">
            <div class="modal-header">
                <h3><i class="fas fa-file-invoice"></i> Chi Tiết Đơn Hàng</h3>
                <button class="close-modal" onclick="closeBillDetailModal()">
                    <i class="fas fa-times"></i>
                </button>
            </div>
            <div class="modal-body" id="billDetailContent">
                <!-- Bill details will be loaded here -->
            </div>
        </div>
    </div>

    <!-- Footer -->
    <footer class="footer">
        <div class="footer-content">
            <div class="footer-section">
                <h4>VỀ CHÚNG TÔI</h4>
                <ul>
                    <li><a href="#">Giới thiệu</a></li>
                    <li><a href="#">Tuyển dụng</a></li>
                    <li><a href="#">Tin tức</a></li>
                    <li><a href="#">Liên hệ</a></li>
                </ul>
            </div>
            <div class="footer-section">
                <h4>HỖ TRỢ KHÁCH HÀNG</h4>
                <ul>
                    <li><a href="#">Hướng dẫn mua hàng</a></li>
                    <li><a href="#">Chính sách bảo hành</a></li>
                    <li><a href="#">Chính sách đổi trả</a></li>
                    <li><a href="#">FAQ</a></li>
                </ul>
            </div>
            <div class="footer-social">
                <h4>THEO DÕI CHÚNG TÔI</h4>
                <div class="social-icons">
                    <a href="#"><i class="fab fa-facebook"></i></a>
                    <a href="#"><i class="fab fa-twitter"></i></a>
                    <a href="#"><i class="fab fa-instagram"></i></a>
                    <a href="#"><i class="fab fa-youtube"></i></a>
                </div>
            </div>
        </div>
        <div class="footer-bottom">
            <p>© 2025 TECH HAVEN. Tất cả quyền được bảo lưu</p>
        </div>
    </footer>

    <!-- Scripts -->
    <script src="/js/script.js"></script>
    <script src="/js/bill_detail_admin.js"></script>
</body>
</html>
